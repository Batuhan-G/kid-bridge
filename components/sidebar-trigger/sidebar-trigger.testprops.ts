import { SidebarTriggerProps } from './sidebar-trigger.types'

export const sidebarTriggerTestProps: SidebarTriggerProps = {
  onToggle: () => {},
  totalStats: {
    events: 2,
    messages: 1,
    expenses: 500,
  },
} 